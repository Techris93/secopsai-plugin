{
  "id": "secopsai",
  "name": "SecOpsAI",
  "description": "Native SecOpsAI findings, triage orchestration, queued actions, and supply-chain investigation tools for OpenClaw.",
  "configSchema": {
    "type": "object",
    "properties": {
      "secopsaiPath": {
        "type": "string",
        "description": "Path to secopsai installation (default: ~/secopsai)",
        "default": "~/secopsai"
      },
      "socDbPath": {
        "type": "string",
        "description": "Path to SOC database",
        "default": "~/secopsai/data/openclaw/findings/openclaw_soc.db"
      }
    },
    "additionalProperties": false
  }
}
